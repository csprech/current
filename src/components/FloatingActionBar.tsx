"use client";

/**
 * Compatibility boundary for integrations that still import the retired strip.
 * Current workspace production composition uses AddPalette instead.
 */
export function FloatingActionBar() {
  return null;
}
